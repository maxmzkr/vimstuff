package storyhandler

import (
  "html/template"
  "net/http"

  "github.com/maxmzkr/handlers"
  "github.com/maxmzkr/thegoodpear/handlers/topbarhandler"
)

type Page struct {
  Title string
  Content template.HTML
}

type Story struct {
  Page
  topbarhandler.Topbar
}

func HomeHandler(writer http.ResponseWriter, request *http.Request) {
  page := Page{Title: "home"}
  topbar := topbarhandler.Topbar{Words: "words"}
  story := Story{page, topbar}

  handlers.ExecuteInGlobal(writer, "story", &story)
}

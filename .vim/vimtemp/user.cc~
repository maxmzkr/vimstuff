#include <string>
#include <unordered_map>
#include "session.h"

class User {
 public:
  User(std::string username, std::string password);

  // Getters
  std::string password();

 private:
  std::unordered_map<unsigned int, Session> session_map_;
  std::string password_;
  std::string name_;
};

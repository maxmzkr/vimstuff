#include "./vm_app.h"

int main() {
  char * p1 = reinterpret_cast<char *>(vm_extend(1));
  char * p = reinterpret_cast<char *>(vm_extend(0));
  while (p != nullptr) {
    if (p > reinterpret_cast<void *>(0x603ff000)) {
      break;
    }
    p = reinterpret_cast<char *>(vm_extend(0));
  }
  p1 = reinterpret_cast<char *>(vm_extend(1));
}

import time
from redis import Redis
from rq import Queue
from modules.count import count_words_at_url, long

redis_conn = Redis()
q = Queue('low', connection=redis_conn, async=False)
job = q.enqueue(long)
print job.result

time.sleep(2)
print job.result

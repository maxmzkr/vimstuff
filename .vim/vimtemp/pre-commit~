#!/bin/bash
#
# Adds the files to submit

non_filtered_files=($(find | egrep "^.*(\.cc|\.h)$"))
while read line; do
	non_filtered_files=(${non_filtered_files[@]/${line}})
done <FILTER
( IFS=$'\n'; echo "${non_filtered_files[*]}" ) > SUBMIT
exit 0

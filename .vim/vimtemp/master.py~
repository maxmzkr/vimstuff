import time
from redis import Redis
from rq import Queue
from modules.count import count_words_at_url

redis_conn = Redis()
q = Queue('low', connection=redis_conn)
job = q.enqueue(count_words_at_url, 'http://nvie.com')
print job.result

time.sleep(2)
print job.result

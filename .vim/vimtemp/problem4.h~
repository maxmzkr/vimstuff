#ifndef PROBLEM4_H_
#define PROBLEM4_H_

struct Router {
  Router * source;
  Router * destination1;
  Router * destination2;
}

const Router * potentiallyBrokenRouter(const Router * router1,
                                       const Router * router2);

#endif  // PROBLEM4_H_
